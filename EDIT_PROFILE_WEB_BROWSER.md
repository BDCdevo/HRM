# تعديل الملف الشخصي عبر المتصفح

## 📋 التغيير

تم تعديل زر **"Edit Profile"** ليفتح صفحة التعديل في المتصفح مباشرة بدلاً من فتح شاشة داخل التطبيق.

---

## 🎯 السبب

صفحة التعديل في الباك إند تحتوي على جميع الحقول والخيارات المتاحة للموظف، بينما شاشة التطبيق كانت محدودة.

---

## ✅ ما تم تنفيذه

### 1. إضافة دالة لفتح الرابط
```dart
Future<void> _openEditProfileInBrowser(int employeeId, int companyId) async {
  final url = Uri.parse('https://erp1.bdcbiz.com/hrm/$companyId/employees/$employeeId/edit');

  try {
    if (await canLaunchUrl(url)) {
      await launchUrl(
        url,
        mode: LaunchMode.externalApplication, // فتح في المتصفح
      );
    } else {
      // عرض رسالة خطأ
      ScaffoldMessenger.of(context).showSnackBar(
        const SnackBar(
          content: Text('Could not open browser'),
          backgroundColor: AppColors.error,
        ),
      );
    }
  } catch (e) {
    // معالجة الأخطاء
    ScaffoldMessenger.of(context).showSnackBar(
      SnackBar(
        content: Text('Error: ${e.toString()}'),
        backgroundColor: AppColors.error,
      ),
    );
  }
}
```

### 2. تعديل زر Edit Profile
```dart
_MenuItem(
  svgIcon: 'assets/svgs/profile_icon.svg',
  title: 'Edit Profile',
  subtitle: 'Edit your profile in web browser', // ✅ نص محدّث
  color: AppColors.secondary,
  cardColor: cardColor,
  textColor: textColor,
  secondaryTextColor: secondaryTextColor,
  onTap: () {
    // ✅ فتح في المتصفح
    final employeeId = user.id;
    final companyId = user.companyId ?? 6; // Default to 6 (BDC)
    _openEditProfileInBrowser(employeeId, companyId);
  },
),
```

### 3. إضافة url_launcher
تم إضافة `import 'package:url_launcher/url_launcher.dart';` في أعلى الملف.

---

## 🌐 الرابط المستخدم

```
https://erp1.bdcbiz.com/hrm/{companyId}/employees/{employeeId}/edit
```

### مثال:
```
https://erp1.bdcbiz.com/hrm/6/employees/56/edit
```

حيث:
- **6** = Company ID (BDC)
- **56** = Employee ID

---

## 📱 كيف يعمل

### الخطوات:
1. المستخدم يضغط على "Edit Profile"
2. التطبيق يقوم بـ:
   - الحصول على `employeeId` من `user.id`
   - الحصول على `companyId` من `user.companyId` (افتراضي: 6)
   - بناء الرابط: `https://erp1.bdcbiz.com/hrm/6/employees/56/edit`
3. التطبيق يفتح الرابط في المتصفح الخارجي
4. المستخدم يقوم بالتعديل في المتصفح
5. بعد الانتهاء، يعود للتطبيق

---

## 🎨 واجهة المستخدم

### قبل الضغط:
```
┌────────────────────────────┐
│  [Icon] Edit Profile       │
│         Edit your profile  │
│         in web browser     │
└────────────────────────────┘
```

### بعد الضغط:
```
[يفتح المتصفح]
↓
https://erp1.bdcbiz.com/hrm/6/employees/56/edit
```

---

## ✅ المميزات

### 1. إمكانيات كاملة
- ✅ جميع حقول التعديل متاحة
- ✅ رفع الملفات والمستندات
- ✅ اختيار من القوائم المنسدلة
- ✅ تعديل معلومات الطوارئ
- ✅ تعديل معلومات الشركة

### 2. أمان
- ✅ استخدام نفس الـ Session
- ✅ التحقق من الصلاحيات في الباك إند
- ✅ لا حاجة لإعادة تسجيل الدخول

### 3. سهولة
- ✅ ضغطة واحدة
- ✅ يفتح في المتصفح مباشرة
- ✅ رجوع سهل للتطبيق

---

## 📝 ملاحظات مهمة

### 1. Company ID الافتراضي
```dart
final companyId = user.companyId ?? 6; // Default to 6 (BDC)
```
- إذا لم يكن `companyId` موجود في UserModel، يستخدم 6 (BDC)

### 2. الأذونات المطلوبة
تأكد من إضافة الأذونات في:

**Android** (`android/app/src/main/AndroidManifest.xml`):
```xml
<queries>
  <intent>
    <action android:name="android.intent.action.VIEW" />
    <data android:scheme="https" />
  </intent>
</queries>
```

**iOS** (`ios/Runner/Info.plist`):
```xml
<key>LSApplicationQueriesSchemes</key>
<array>
  <string>https</string>
  <string>http</string>
</array>
```

### 3. معالجة الأخطاء
- إذا فشل فتح المتصفح، يعرض رسالة خطأ
- إذا حدث exception، يعرض الخطأ للمستخدم

---

## 🔧 الملفات المعدّلة

| الملف | التغيير |
|-------|---------|
| `more_main_screen.dart` | إضافة دالة + تعديل onTap ✅ |

---

## 🚀 الاستخدام

### من المستخدم:
1. افتح تبويب "More"
2. اضغط على "Edit Profile"
3. ينفتح المتصفح تلقائياً
4. قم بالتعديلات المطلوبة
5. احفظ
6. ارجع للتطبيق

### من المطور:
```dart
// الدالة جاهزة - فقط استدعيها
_openEditProfileInBrowser(employeeId, companyId);
```

---

## 🆚 المقارنة

| الميزة | قبل (شاشة التطبيق) | بعد (المتصفح) |
|--------|---------------------|----------------|
| عدد الحقول | 2-3 حقول | جميع الحقول |
| رفع الملفات | محدود | كامل |
| سهولة التعديل | متوسطة | عالية |
| إمكانيات | محدودة | كاملة |
| الصيانة | صعبة | سهلة |

---

## 🎯 الخلاصة

### قبل ❌:
- شاشة داخل التطبيق
- حقول محدودة
- صعوبة في الصيانة

### بعد ✅:
- فتح في المتصفح
- جميع الحقول متاحة
- صيانة سهلة (كل شيء في الباك إند)

---

## 📚 Dependencies

تأكد من وجود `url_launcher` في `pubspec.yaml`:

```yaml
dependencies:
  url_launcher: ^6.2.2
```

إذا لم يكن موجوداً، قم بتشغيل:
```bash
flutter pub add url_launcher
flutter pub get
```

---

**الملف**: `lib/features/more/ui/screens/more_main_screen.dart`
**التاريخ**: 2025-11-23
**الإصدار**: 2.0 (Web Browser)
**الحالة**: ✅ جاهز للاستخدام

🎉 **الآن المستخدم يستطيع تعديل ملفه الشخصي بالكامل من خلال المتصفح!**
