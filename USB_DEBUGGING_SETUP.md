# 🔌 دليل توصيل الهاتف بالكمبيوتر (USB Debugging)

**التاريخ:** 2025-11-10
**الغرض:** تشغيل تطبيق Flutter على هاتف حقيقي

---

## 📱 **الخطوة 1: تفعيل Developer Options**

### **على الهاتف:**

```
1. اذهب إلى: Settings (الإعدادات)
2. اذهب إلى: About Phone (حول الهاتف)
3. ابحث عن: "Build Number" (رقم الإصدار)
4. اضغط 7 مرات متتالية على "Build Number"
5. سترى رسالة: "You are now a developer!" (أنت الآن مطور!)
```

**ملاحظة:**
- بعض الهواتف: `Settings > System > About Phone`
- Xiaomi: `Settings > About Phone > MIUI Version` (اضغط 7 مرات)
- Samsung: `Settings > About Phone > Software Information > Build Number`

---

## 🔓 **الخطوة 2: تفعيل USB Debugging**

### **على الهاتف:**

```
1. ارجع للإعدادات: Settings
2. اذهب إلى: System > Developer Options
   (أو: Additional Settings > Developer Options)
3. فعّل: "USB Debugging" ✅
4. (اختياري) فعّل: "Install via USB" ✅
```

---

## 🔌 **الخطوة 3: توصيل الكابل**

```
1. وصّل كابل USB من الهاتف للكمبيوتر
2. على الهاتف، اختار نوع الاتصال:
   - "File Transfer" (MTP) ✅ موصى به
   - أو "Transfer Files"

   ❌ تجنب: "Charge Only"
```

---

## ✅ **الخطوة 4: السماح بـ USB Debugging**

### **سيظهر على الهاتف:**

```
┌─────────────────────────────────────┐
│  Allow USB debugging?               │
│                                     │
│  The computer's RSA key             │
│  fingerprint is:                    │
│  AB:CD:EF:12:34...                 │
│                                     │
│  ✅ Always allow from this computer│
│                                     │
│  [Cancel]  [OK]                    │
└─────────────────────────────────────┘
```

**اضغط:**
1. ✅ فعّل "Always allow from this computer"
2. اضغط **OK**

---

## 🧪 **الخطوة 5: التحقق من الاتصال**

### **على الكمبيوتر (Terminal):**

```bash
adb devices
```

### **المخرجات المتوقعة:**

```
List of devices attached
ABC123XYZ    device
```

✅ إذا رأيت "device" → ممتاز! الهاتف متصل
❌ إذا رأيت "unauthorized" → ارجع للخطوة 4 واسمح من الهاتف
❌ إذا لم يظهر شيء → راجع استكشاف الأخطاء أدناه

---

## ❌ **استكشاف الأخطاء:**

### **المشكلة 1: الهاتف لا يظهر في `adb devices`**

**الحلول:**

#### **أ) تحقق من تعريفات USB:**

```bash
# على Windows:
# افتح Device Manager (إدارة الأجهزة)
# ابحث عن "Android Device" أو اسم هاتفك
# إذا كان عليه علامة ! صفراء:
# Right-click > Update Driver > Search automatically
```

#### **ب) أعد تشغيل ADB Server:**

```bash
adb kill-server
adb start-server
adb devices
```

#### **ج) جرب منفذ USB آخر:**

```
- استخدم منفذ USB مختلف على الكمبيوتر
- استخدم كابل USB آخر (بعض الكابلات للشحن فقط!)
```

#### **د) أعد تشغيل الهاتف والكمبيوتر:**

```
1. افصل الكابل
2. أغلق USB Debugging على الهاتف
3. أعد تشغيل الهاتف
4. أعد تشغيل الكمبيوتر
5. فعّل USB Debugging مرة أخرى
6. وصّل الكابل
```

---

### **المشكلة 2: يظهر "unauthorized"**

```
List of devices attached
ABC123XYZ    unauthorized
```

**الحل:**

```
1. على الهاتف: Settings > Developer Options
2. أغلق ثم فعّل "USB Debugging" مرة أخرى
3. افصل الكابل وأعد توصيله
4. اسمح بـ USB Debugging عندما يظهر الطلب
5. فعّل "Always allow from this computer"
```

---

### **المشكلة 3: يظهر "offline"**

```
List of devices attached
ABC123XYZ    offline
```

**الحل:**

```bash
# 1. أعد تشغيل ADB
adb kill-server
adb start-server

# 2. أعد توصيل الكابل

# 3. على الهاتف، غيّر نوع الاتصال:
# Settings > USB Preferences > File Transfer
```

---

## 🚀 **بعد الاتصال الناجح:**

### **اختبر الاتصال:**

```bash
# 1. تحقق من الأجهزة المتصلة
flutter devices

# 2. شغل التطبيق على الهاتف
flutter run

# 3. أو حدد الجهاز بالـ ID
flutter run -d ABC123XYZ
```

---

## 📝 **نصائح مهمة:**

### **1. كابل USB:**
```
⚠️ استخدم كابل USB للبيانات (Data Cable)
❌ بعض الكابلات للشحن فقط ولن تعمل
✅ استخدم الكابل الأصلي للهاتف إن أمكن
```

### **2. Developer Options قد تختفي:**
```
إذا اختفت Developer Options بعد إعادة تشغيل الهاتف:
- كرر الخطوة 1 (اضغط 7 مرات على Build Number)
```

### **3. أمان USB Debugging:**
```
⚠️ USB Debugging يسمح للكمبيوتر بالتحكم في الهاتف
✅ فقط فعّله مع أجهزة كمبيوتر موثوقة
✅ أغلقه بعد انتهاء التطوير
```

---

## ✅ **Checklist:**

قبل ما تبلّغ عن المشكلة، تأكد من:

- [ ] Developer Options مفعّلة (اضغطت 7 مرات على Build Number)
- [ ] USB Debugging مفعّل في Developer Options
- [ ] الكابل موصول والهاتف يشحن
- [ ] نوع الاتصال: "File Transfer" (ليس "Charge Only")
- [ ] سمحت بـ USB Debugging على الهاتف (Allow)
- [ ] `adb devices` يعرض الهاتف كـ "device"
- [ ] جربت منفذ USB آخر أو كابل آخر

---

## 🎯 **للمتابعة:**

بعد الاتصال الناجح:

```bash
# شغل التطبيق
flutter run

# راقب الـ console للبحث عن errors:
# - SocketException
# - HandshakeException
# - Connection refused
```

---

**إذا لا يزال الهاتف لا يظهر، أرسل:**
1. نوع الهاتف (Xiaomi, Samsung, etc.)
2. Android Version
3. screenshot من Developer Options
4. output من: `adb devices -l`
